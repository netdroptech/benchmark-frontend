# Used by `vite build` when NODE_ENV=production (Render does this automatically).
# IMPORTANT: replace the URL below with the public URL of your backend on Render
# *after* you've deployed the API web service. The trailing /api is required.
VITE_API_URL=https://YOUR-BACKEND-NAME.onrender.com/api
